name: HACS Action

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  hacs:
    name: HACS Action
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v3.2.0"
      - name: HACS Action
        uses: "hacs/action@main"
        with:
          category: "integration"
